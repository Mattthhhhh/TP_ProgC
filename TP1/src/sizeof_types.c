#include <stdio.h>
#include <string.h>

int main() {
  printf, (sizeof(char));
  printf, (sizeof(short));
  printf, (sizeof(int));
  printf, (sizeof(long int));
  prinft, (sizeof(long long int));
  printf, (sizeof(float));
  printf, (sizeof(double));
  printf, (sizeof(long double));
return 0;
}
  
  
